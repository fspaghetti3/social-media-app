<!DOCTYPE html>
<html>

<head>
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('https://images.unsplash.com/photo-1505506874110-6a7a69069a08?w=500&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8NHx8c3BhY2V8ZW58MHx8MHx8fDA%3D');
            background-size: cover;
            background-position: center;
        }

        .hover-shadow:hover {
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        }

    </style>
</head>

<body class="bg-gray-200 min-h-screen flex flex-col items-center">
    <div class="text-center mt-10 mb-8">
        <h1 class="text-8xl text-white font-bold">Welcome back, {{ username }}!</h1>
    </div>

    <div class="flex flex-col space-y-4 w-1/6 fixed left-8">
        <a href="/posts/create" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mb-2 inline-block hover-shadow">Create Blog Post</a>
        <a href="/profile" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded mb-2 inline-block hover-shadow">Profile</a>
        <a href="/users/logout" class="bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded mb-2 inline-block hover-shadow">Logout</a>
    </div>

    <div class="w-3/5 mx-auto mt-16">
  <h1 class="text-4xl text-blue-700 font-bold border-b-4 border-blue-700 pb-4">Latest Posts</h1>
  {{#each posts}}
  <div id="{{this.id}}" class="post-item w-full bg-white shadow-md rounded border-4 border-blue-400 px-4 py-3 mb-4">
      {{#if this.image}}
          <img src="{{this.image}}" alt="{{this.title}}">
      {{else}}
          <video src="{{this.video}}" controls class="w-full h-auto"></video>
      {{/if}}
            <div class="relative flex justify-between reactions">
                <div class="hidden justify-center absolute -top-7 z-40 p-1 border-solid border-black">
                    <span class="mr-2 likes">&#x1F44D</span>
                    <span class="dislikes">&#x1F44E</span>
                </div>
                <button class="text-center p-1 like border border-solid border-2 bg-grey-400 mt-2 rounded px-5 w-1/2">{{this.likes}} likes</button>
                <a href="/posts/{{this.id}}" class="text-center p-1 like border border-solid border-2 bg-grey-400 mt-2 rounded px-5 text-blue-500 hover:underline">Comment  <span class="text-green-600">{{this.comments.length}}</span></a>
            </div>
            <h2 class="text-lg font-semibold mb-2 text-blue-700">{{this.title}}</h2>
            <p class="text-gray-600">By: {{this.user.username}}</p>
            <p class="text-gray-700 mb-2">{{this.content}}</p>
        </div>
        {{/each}}
    </div>
    <script src="/js/video.js"></script>
    <script src="/js/likes.js"></script>
</body>

</html>