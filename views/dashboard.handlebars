<!DOCTYPE html>
<html>

<head>
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-200 h-screen">
    <div class="text-center mb-8">
        <h1 class="text-8xl text-white font-bold mb-8">Welcome back, {{ username }}!</h1>
    </div>

    <div class="text-center">
        <a href="/posts/create" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mr-4 mb-2 inline-block">Create Blog Post</a>
        <a href="/edit/profile" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded mb-2 inline-block">Profile</a>
        <a href="/users/logout" class="bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded mr-4 mb-2 inline-block">Logout</a>

    </div>
    <div class="w-full text-center p-8">
        <h1 class="text-4xl text-blue-700 font-bold border-b-4 border-blue-700 pb-4">Latest Posts</h1>
    </div>

    <div class="w-96 mx-auto">
        {{#each posts}}
        <div id="{{this.id}}" class="post-item  w-4/5 bg-white shadow-md rounded border-4 border-blue-400 px-4 py-3 mb-4">
        {{#if this.image}}
        <img src="{{this.image}}" alt="{{this.title}}">
        {{else}}
        <video src="{{this.video}}" controls></video>
        {{/if}}
            <div class="relative flex justify-between reactions">
                <div class="hidden justify-center absolute -top-7 z-40 p-1 border-solid border-black">
                    <span class="mr-2 likes">&#x1F44D</span>
                    <span class="dislikes">&#x1F44E</span>
                </div>
                <button class="text-center p-1 like border border-solid border-2 bg-grey-400 mt-2 rounded px-5 w-1/2">{{this.likes}} likes</button>
                <a href="/posts/{{this.id}}" class="text-center p-1 like border border-solid border-2 bg-grey-400 mt-2 rounded px-5 text-blue-500 hover:underline">Comment  <span class="text-green-600">{{this.comments.length}}</span></a>
            </div>
            <h2 class="text-lg font-semibold mb-2 text-blue-700">{{this.title}}</h2>
            <p class="text-gray-600">By: {{this.user.username}}</p>
            <p class="text-gray-700 mb-2">{{this.content}}</p>
            
        </div>
        {{/each}}
    </div>
    <script src="/js/video.js"></script>
    <script src="/js/likes.js"></script>
</body>

</html>